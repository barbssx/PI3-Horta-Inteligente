[build]
builder = "Dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/"
restartPolicyType = "on_failure"

[env]
PORT = "3000"
NODE_ENV = "production"
